// Widget tests for ResQLink Dashboard.
// The root widget is DashboardApp (backed by GoRouter + Firebase).
// Full integration tests require a Firebase emulator and are kept in
// the integration_test/ directory. This file is kept as a placeholder.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder – real tests live in integration_test/', () {
    expect(true, isTrue);
  });
}
